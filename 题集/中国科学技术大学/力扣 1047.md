# 力扣 1047

## 来源

- 本仓库 机试练习/2026.7.15/力扣 1047. 删除字符串中的所有相邻重复项（栈） 2026中科大aids夏令营 （第三题）`r

## 题目与题解

//https://leetcode.cn/problems/remove-all-adjacent-duplicates-in-string/
class Solution {
public:
    string removeDuplicates(string s) {
        stack<char> st;
        for(int i=0;i<s.size();i++){
            if(!st.empty()){
                if(s[i]==st.top()){
                    st.pop();
                    continue;
                }
            }
            st.push(s[i]);
        }
        string ans="";
        while(!st.empty()){
            ans+=st.top();
            st.pop();
        }
        reverse(ans.begin(),ans.end());
        return ans;
    }
};


