# 力扣 56

## 来源

- 本仓库 机试练习/2026.7.15/力扣 56.合并区间（排序） 2026中科大aids夏令营（第四题））`r

## 题目与题解

//https://leetcode.cn/problems/merge-intervals/
class Solution {
public:
    vector<vector<int>> merge(vector<vector<int>>& intervals) {
        if(intervals.size()==1){
            return intervals;
        }
        sort(intervals.begin(),intervals.end());
        vector<vector<int>> ans;
        int l=intervals[0][0];
        int r=intervals[0][1];
        for(int i=1;i<intervals.size();i++){
            if(intervals[i][0]<=r){
                r=max(r,intervals[i][1]);
            }
            else{
                ans.push_back({l,r});
                l=intervals[i][0];
                r=intervals[i][1];
            }
            if(i==intervals.size()-1){
                ans.push_back({l,r});
                break;
            }
        }
        return ans;
    }
};


